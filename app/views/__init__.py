"""The workbench window's views (plan 3B): top bar, review queue, stage,
inspector, activity strip and the open-folder empty state. Views read the
model and call commands through `app.controller.ProjectController` only.
"""
