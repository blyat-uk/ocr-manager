# Runtime dependencies
PyQt6>=6.5.0
opencv-python>=4.8.0
numpy>=1.24.0

# Build dependencies (optional - only needed to create executables)
# pyinstaller>=6.0.0

# External tools required (not Python packages):
# - ffmpeg (must be in PATH)
# - ffprobe (must be in PATH, usually comes with ffmpeg)
#
# CLI Tools for full OCR pipeline (optional):
# - ocrp, ass-credits, ass-header, ass-qafix, sub-visualize
# - subs-translator, submerge, srt-to-ass
