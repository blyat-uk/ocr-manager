# Runtime dependencies
PyQt6>=6.5.0
qtawesome>=1.3.0
qt-material>=2.14
opencv-python>=4.8.0
numpy>=1.24.0
scipy>=1.14.0

# videocr dependencies (now embedded)
# Note: paddlepaddle-gpu must be installed separately via Paddle's index:
#   .venv/bin/pip install paddlepaddle-gpu==3.3.0 -i https://www.paddlepaddle.org.cn/packages/stable/cu129/
paddleocr==3.3.2
paddlex==3.3.10
thefuzz==0.22.1
python-Levenshtein==0.27.3
PyYAML==6.0.2
packaging==25.0
# Pinned exactly: an av bump changes the bundled swscale (output can move
# silently) and the wheel's filter inventory (the PyAV tone-map chain is
# chosen from it). Re-pin deliberately, then re-run tools/fidelity_check.py.
av==18.1.0
setuptools>=61.0
rapidfuzz>=3.0.0
jieba3>=1.0.0

# Build dependencies (optional - only needed to create executables)
# pyinstaller>=6.0.0

# External tools required (not Python packages):
# - ffmpeg (must be in PATH)
# - ffprobe (must be in PATH, usually comes with ffmpeg)
